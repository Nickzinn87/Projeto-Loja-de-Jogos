<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Projeto2W1N.View.CadPage"
             Title="CadPage">
    <ScrollView>
        <StackLayout>
            <Label Text="Cadastrar"
               FontSize="30"
               HorizontalOptions="Center"
               Margin="0,20,0,20"/>

            <Button Text="Voltar"
                    Margin="20, 0, 20, 0"
                    Command="{Binding VoltarCommand}"/>

            <Label Text="Nome"
                   FontSize="20"
                   Margin="0,5,0,5"/>
            <Entry x:Name="txtNome"
                   Text="{Binding Nome}"
                   Margin="0,10,0,10"/>
            <Label Text="Email"
                   FontSize="20"
                   Margin="0,5,0,5"/>
            <Entry x:Name="txtEmail"
                   Text="{Binding Email}"
                   Margin="0,10,0,10"/>
            <Label Text="Senha"
                   FontSize="20"
                   Margin="0,5,0,5"/>
            <Entry x:Name="txtSenha"
                   Text="{Binding Senha}"
                   Margin="0,10,0,10"
                   IsPassword="True"/>
            <Label Text="CPF"
                   FontSize="20"
                   Margin="0,5,0,5"/>
            <Entry x:Name="txtCPF"
                   Text="{Binding CPF}"
                   Margin="0,10,0,10"/>
            <StackLayout Orientation="Horizontal"
                     VerticalOptions="Center"
                     HorizontalOptions="Center">
                <CheckBox x:Name="ckbAdmin"
                          IsChecked="{Binding Admin}"
                          VerticalOptions="Center"
                          Margin="5,5,0,10"/>
                <Label Text="Cadastrar Administrador"
                   VerticalOptions="Center"
                   Margin="0,5,5,10"/>
            </StackLayout>
            <Button Text="Cadastrar"
                Margin="0,10,0,10"
                Command="{Binding CadastrarCommand}"/>
            

        </StackLayout>
    </ScrollView>
</ContentPage>